export const mixins = {
      // 默认配置
      defaultConfig: {
        // 节点配置
        node: {
          // 节点大小
          size: 30,
          // 节点颜色
          color: '#409EFF',
          // 节点文本颜色
          textColor: '#fff',
          // 节点文本大小
          textSize: 12,
      }
  }
}